# 本地图生图（Diffusers + MPS/CUDA/CPU），与 LangGraph 依赖分开安装：
#   cd agent && source .venv/bin/activate && pip install -r requirements-img2img.txt
torch>=2.2.0
diffusers>=0.31.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.0
Pillow>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
